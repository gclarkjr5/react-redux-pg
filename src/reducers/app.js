
export default (state={}, action) => {
    switch(action.type) {
        // case 'LOAD GREETING':
        //     return {...state, greeting: action.payload.greeting}

        default:
            return state
    }
}